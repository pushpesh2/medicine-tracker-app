name: Build Android APK (Ultra Fast)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Hash app source files to detect changes
      - name: Calculate source hash
        id: hash
        run: |
          echo "HASH=$(find . -type f \( -name '*.py' -o -name '*.kv' -o -name '*.spec' -o -name '*.png' \) -print0 | sort -z | xargs -0 sha256sum | sha256sum | cut -d' ' -f1)" >> $GITHUB_OUTPUT

      # Restore Buildozer cache if available
      - name: Restore Build Cache
        id: restore
        uses: actions/cache@v4
        with:
          path: .buildozer
          key: buildozer-${{ steps.hash.outputs.HASH }}
          restore-keys: |
            buildozer-

      # Check if build is needed
      - name: Check if build needed
        id: check
        run: |
          if [ -d ".buildozer" ] && [ -f ".buildozer/build_complete" ]; then
            echo "BUILD_NEEDED=false" >> $GITHUB_OUTPUT
          else
            echo "BUILD_NEEDED=true" >> $GITHUB_OUTPUT
          fi

      # Build APK using Docker if needed
      - name: Build APK
        if: steps.check.outputs.BUILD_NEEDED == 'true'
        uses: addnab/docker-run-action@v3
        with:
          image: kivy/buildozer:latest
          options: --workdir /app -v ${{ github.workspace }}:/app
          run: |
            set -e
            export BUILD_NO_INTERACTIVE=1
            export BUILD_USER=root
            buildozer android debug
            mkdir -p /app/dist
            cp bin/*/*.apk /app/dist/
            touch .buildozer/build_complete

      # Upload APK artifact
      - name: Upload APK
        if: steps.check.outputs.BUILD_NEEDED == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: MedicineTracker-APK
          path: dist/*.apk