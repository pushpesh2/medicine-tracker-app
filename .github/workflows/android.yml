name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build APK (Docker – permission safe)
        uses: addnab/docker-run-action@v3
        with:
          image: kivy/buildozer:latest
          options: |
            -v ${{ github.workspace }}:/workspace
            -w /workspace
          run: |
            set -e

            # Create writable dirs INSIDE workspace
            mkdir -p .buildozer_local
            mkdir -p .build_cache

            # IMPORTANT: force buildozer dirs via flags
            buildozer \
              --build-dir .build_cache \
              --user-data-dir .buildozer_local \
              android debug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: MedicineTracker-APK
          path: bin/*.apk
          if-no-files-found: error